server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:auditdemo;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  sql:
    init:
      mode: always
mybatis:
  configuration:
    map-underscore-to-camel-case: true
